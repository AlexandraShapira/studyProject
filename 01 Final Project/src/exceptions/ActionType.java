/** 
 * The enum class lists the actions for Exceptions' constructors.
 * @author Alexandra Shapira
 */

package exceptions;

public enum ActionType {

		CREATE,
		GET,
		UPDATE,
		REMOVE,
		JOIN;
		
}
